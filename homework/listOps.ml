(* Duplicate the elements of a list *)
let duplicate list = 
(* to be written *)

(* Reverse a list *)
let rev list =
(* to be written *)

(* Find out whether a list is a palindrome *)
let is_palindrome list =
(* to be written *)