#lang racket

(define (flatten-list list)
  (error 'verify "not yet implemented!"))

(define (cartesian-product l1 l2)
  (error 'verify "not yet implemented!"))


(define (zigzag n)
  (error 'verify "not yet implemented!"))

(define (encrypt s)
  (error 'verify "not yet implemented!"))


(define (decrypt s)
  (error 'verify "not yet implemented!"))
